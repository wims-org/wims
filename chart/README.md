# WIMS Helm Chart

Please note, that currently a treafik is needed at your cluster and will be configured to forward / and /api routes.

Customize your values.<instance>.yaml file and render the helm templates: 

```shell
helm template .
```
