[project]
name = "wims-cli"
version = "0.1.0"
description = "A cli client for WIMS"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "prettytable>=3.17.0",
    "pyyaml>=6.0.3",
    "xdg>=6.0.0",
]
